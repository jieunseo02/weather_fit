# 데이터셋 카탈로그 (weather_fit)

멘토가 백필해 제공하는 데이터셋 정의서. 각 소스의 **엔드포인트 / 인증 / 백필 가능 여부 / 한도 / 저장 경로**를 확정한다.

- 대상 지역: **서울** (ASOS 지점 `108`, 예보관서 `109`, 중기육상 `11B00000`, 중기기온 `11B10101`)
- 대상 기간: **2023-01-01 ~ 2026-07-31** (3년, 코로나 기간 제외)
- 저장 포맷: Parquet, 월 파티션 `{yyyy-mm}.parquet`

---

## 0. 한눈에 보기

| ID | 데이터셋 | 역할 | 소스 | 백필 | 우선 |
|---|---|---|---|---|---|
| `W1` | 지상관측 일자료 (ASOS) | **기상 실황 = 분석 기준** | KMA API Hub | ✅ 3년 | P0 |
| `W2` | 중기 기온예보 (발표분 아카이브) | 모델 입력 = "내일 예보" | KMA API Hub | ✅ 2012-12~ | P0 |
| `W3` | 중기 육상예보 (하늘/강수확률) | 모델 입력 | KMA API Hub | ✅ 2012-12~ | P0 |
| `W4` | 기상특보 발표 이력 | 이벤트 라벨 (ground truth) | KMA API Hub | ✅ 2004-06~ | P0 |
| `T1` | **쇼핑인사이트 카테고리 클릭지수** | **메인 타깃** | 네이버 데이터랩 | ✅ 2017-08~ | P0 |
| `T2` | 쇼핑인사이트 카테고리 × 성별·연령·기기 | 타깃 분해 | 네이버 데이터랩 | ✅ | P1 |
| `T3` | **쇼핑인사이트 키워드 클릭지수** (30개 날씨민감어) | **세분 타깃** | 네이버 데이터랩 | ✅ | P0 |
| `T4` | **검색어트렌드** (패션 키워드 그룹) | 보조 타깃 / 선행지표 | 네이버 데이터랩 | ✅ 2016-01~ | P1 |
| `T5` | 카테고리 인기검색어 TOP500 (순위) | 순위형 타깃 | 데이터랩 웹 내부 API | ⚠️ 비공식 | P2 |
| ~~`T6`~~ | ~~쇼핑 검색 상위 노출 상품~~ | — | ~~네이버 검색 API~~ | ❌ **API 종료** | 폐기 |
| `T7` | 구글 트렌드 (**서울 지역** 분해) | 지역 검증 / 교차 검증 | pytrends | ✅ | P1 |
| `C1` | 공휴일 | 캘린더 피처 | 공공데이터포털 | ✅ | P1 |
| `C2` | 미세먼지 일평균 | 보조 피처 | 에어코리아 | ✅ CSV | P2 |
| `C3` | 온라인쇼핑동향 (의복, 월별 거래액) | **검증용 ground truth** | KOSIS | ✅ | P2 |
| `Q1` | 무신사 매거진·랭킹 | 정성 트렌드 (Q4) | 크롤링 | ⚠️ ToS | P3 |

`T5`·`T6`·`Q1`은 공식 API가 아니거나 백필이 불가능하다. **막히면 우회하지 말고 제외**한다.

---

## 1. 기상 데이터 (기상청 API 허브)

전부 `authKey` 하나로 조회한다. 공공데이터포털(data.go.kr)과 **인증체계가 완전히 별개**이므로 키를 혼용하지 말 것.

베이스: `https://apihub.kma.go.kr/api/typ01/url/`

### W1. 지상관측 일자료 (ASOS) — 분석의 기준선

```
GET kma_sfcdd3.php?tm1=20230101&tm2=20230131&stn=108&disp=1&help=0&authKey={KEY}
```

- **기간 조회 최대 31일** → 3년 백필 = 월 단위 43회 호출
- ⚠️ **`kma_sfcdd3.php` 는 `disp` 를 무시하고 항상 공백 구분으로 응답한다.** 반면 중기예보(`fct_afs_*`)는 `disp=1` 에서 쉼표 구분 + 행 끝 `=` 마커다. **파라미터를 믿지 말고 응답에 쉼표가 있는지로 분기**할 것
- ⚠️ **결측 표기 `-9.0` / `-99.0` 을 일괄 치환하면 실제 관측값이 파괴된다.** 서울 1월 `TA_MIN=-9.0` 은 진짜 -9.0℃다. 컬럼의 물리적 정의역으로 분기해야 한다 — 기온류(`TA_`/`TD_`/`TS_`/`TG_`)는 `-99` 계열만 결측, 음수가 불가능한 컬럼(강수·습도·풍속·적설)은 `-9` 계열도 결측
- ⚠️ **`RN_DAY = -9.0` 은 결측이 아니라 무강수다** (1,308일 중 754일, 57.6%). `processed` 단계에서 `0.0` 으로 채운다. raw에서 채우면 진짜 결측과 영영 구분할 수 없어 그대로 둔다
- 주요 항목: `TA_AVG`(평균기온) `TA_MAX` `TA_MIN` `RN_DAY`(일강수) `HM_AVG`(습도) `WS_AVG`(풍속) `CA_TOT`(운량)
- 이 데이터가 `weather_daily` 의 실체다. **일교차·Δt·체감온도는 여기서 파생**시킨다.

> 예보(W2·W3)가 아니라 **관측(W1)이 EDA와 이벤트 정의의 기준**이다. 예보는 모델 입력일 뿐이다.

### W2·W3. 중기예보 발표분 아카이브 — "그날 알 수 있었던 정보"

```
GET fct_afs_wc.php?tmfc1=2023010106&tmfc2=2023013118&reg=11B10101&disp=1&authKey={KEY}   # 기온
GET fct_afs_wl.php?tmfc1=...&tmfc2=...&reg=11B00000&disp=1&authKey={KEY}                 # 육상(하늘/강수확률)
```

- 발표시각(`tmfc`) 기준 과거 조회 가능, **보유 2012-12-18 18:00 KST ~ 현재**
- 중기예보는 **1일 2회(06/18 KST) 발표**, 3~10일 후를 예보
- 응답: `MIN`/`MAX`(기온), `SKY`, `PRE`, `RN_ST`(강수확률), `WF`
- 서울 기온구역 `11B10101`, 육상구역 `11B00000`(서울·인천·경기)

> **이 데이터셋이 이 프로젝트의 차별점이다.** 관측치로 학습한 모델은 "내일 실제 기온"을 아는 셈이라 실전에서 못 쓴다. 발표분 아카이브를 쓰면 **예보 오차까지 포함한 현실적인 모델**이 된다.
>
> ✅ 과거 발표분 조회 **가능 확인** (2023-01-01~2026-07-31 실제 백필 완료, 2,616 발표분 = 1,308일 × 2회).
>
> ⚠️ **조인 규칙 정정**: 중기예보의 최소 리드타임은 **3일**이다(실측 2.2~9.8일). 따라서 "발표일 D-1 → 대상일 D" 조인은 **성립하지 않는다 — 그런 행이 아예 없다.** 올바른 규칙은 대상일 D에 대해 `tmfc < D` 인 발표분 중 **가장 최신**을 고르는 것이고, 실제로는 D-3 발표가 선택된다. 이것이 "그날 실제로 알 수 있었던 예보"다.
>
> ⚠️ `MOD=A02`(12시간)는 같은 대상일에 00시·12시 두 행을 만든다. 일 단위로 접는 규칙(최고/최저/평균 중 무엇)을 transform에서 정해야 한다.
>
> ⚠️ `fct_afs_wc.php` 는 **기상청 문서 자체에 번호 오타**가 있다 — `MAX`/`MIN_L`/`MIN_H`/`MAX_L`/`MAX_H` 가 전부 "11."로 매겨져 있어, 번호 목록을 믿으면 5개 컬럼이 사라진다. `fct_afs_wl.php` 도 문서 14개 vs 실제 출력 11개다(`MAN_ID`/`MAN_FC`/`REG_NAME` 미출력). **헤더 주석의 마지막 컬럼명 라인을 파싱**해야 한다.

### W4. 기상특보 발표 이력 — 이벤트 라벨의 정답지

```
GET wrn_reg.php?authKey={KEY}                                       # 특보구역 코드 조회 (서울 구역코드 확인용)
GET wrn_met_data.php?tmfc1=20230101&tmfc2=20231231&reg={서울}&authKey={KEY}
```

- **보유 2004-06-30 ~ 현재**. `reg` 파라미터는 **서버측 필터로 정상 동작**한다(전국 2,318행 → 권역 8행)
- 실제 컬럼 순서: `TM_FC, TM_EF, TM_IN, STN, REG_ID, WRN, LVL, CMD, GRD, CNT, RPT, T01~T18` — **문서 순서와 다르다**
- 특보종류(`WRN`): `C`한파 `H`폭염 `R`호우 `S`대설 `D`건조 `W`강풍
- 등급(`LVL`): `1`주의 `2`주의보 `3`경보
- 명령(`CMD`): `1`발표 `3`해제 `6`변경

> **문서 오류 3건 — 실측으로 확정한 것들**
>
> 1. `wrn_table.html` 의 `C=대설, D=한파` 는 **틀렸다.** 3년 실측 분포로 확정: `C`=한파(143건, 겨울 84.6%·여름 0%), `H`=폭염(216건, 여름 84.3%), `S`=대설(겨울 81.5%), `D`=건조.
> 2. **`TM_ED`(해제시각) 컬럼은 없다.** 해제는 `CMD=3` 인 **별도 행**이다(발표 710 / 해제 536 / 변경 132). 발효구간은 발표–해제 행을 짝지어 transform에서 조립해야 한다.
> 3. **서울 광역코드 `L1100000` 은 4년간 0행이다.** 특보는 2020-05-14부터 4개 권역(`L1100100`동남 `L1100200`동북 `L1100300`서남 `L1100400`서북)으로만 발표된다. 또 `wrn_reg.php` 에 '서울'이란 이름이 둘 있고 앞엣것(`L1010100`)은 2020년 만료분이라, **이름으로 집으면 역시 0행**이 된다. 유효기간 필터 + 하위권역 자동 포함으로 해결한다.
>
> → 서울 전체에 특보가 있었는가를 정의하려면 4개 권역을 어떻게 집계할지(any / 과반 / 권역별 유지) 결정해야 한다. 분석 설계 이슈다.
>
> **활용 포인트**: PRD의 룰 기반 이벤트 정의(한파 `t_min ≤ -12℃` 2일 지속 등)를 실제 특보 발표와 대조하면 "우리 룰이 얼마나 맞는가"를 정량 검증할 수 있다. 원안에 없던 분석이고, 리포트에서 가장 설득력 있는 장면이 된다.

---

## 2. 타깃 데이터 (패션 수요)

> ### ⚠️ 2026-07-31 네이버 API 개편 — 착수 전 반드시 확인
>
> | 대상 | 결과 |
> |---|---|
> | 쇼핑 검색 `/v1/search/shop.json` | **완전 종료. 공식 대체 없음** → `T6` 폐기 |
> | 데이터랩 (쇼핑인사이트·검색어트렌드) | **NAVER API HUB로 이관** |
> | 기존 개발자센터 방식 | 유예 대상자에 한해 **2027-06-30까지 동작** |
>
> 이관 후 바뀌는 것 — 도메인 `openapi.naver.com` → `naverapihub.apigw.ntruss.com`,
> 인증 헤더 `X-Naver-Client-*` → `X-NCP-APIGW-API-KEY-ID` / `X-NCP-APIGW-API-KEY`,
> 키는 네이버클라우드 플랫폼에서 **새로 발급**(기존 키 재사용 불가).
> 한도는 쇼핑인사이트·검색어트렌드 각 **월 50,000건**, 50 RPS.
>
> **판단**: 8주 프로젝트는 유예 기한 안에 끝난다. `scripts/check_keys.py` 로 기존 방식이
> 동작하는지 먼저 확인하고, 되면 그대로 진행한다. 안 되면 HUB 키를 발급한다.
> 아래 T1~T4의 요청 body·응답 구조는 두 방식이 같고, **도메인과 헤더만 다르다.**
> 수집 코드에서 이 둘을 상수로 분리해 두면 이관 시 한 줄 교체로 끝난다.

### T1. 쇼핑인사이트 — 카테고리별 클릭지수 ★ 메인 타깃

```
POST https://openapi.naver.com/v1/datalab/shopping/categories
Headers: X-Naver-Client-Id, X-Naver-Client-Secret, Content-Type: application/json
Body: {
  "startDate": "2023-01-01", "endDate": "2026-07-31", "timeUnit": "date",
  "category": [ {"name": "여성의류", "param": ["50000167"]}, ... ]   // 최대 3개
}
```

- **요청당 카테고리 최대 3개**, `timeUnit`: `date` | `week` | `month`
- 데이터 보유 2017-08~ → 3년 백필 문제없음
- 선택 필터: `device`(pc/mo), `gender`, `ages`
- **지역 파라미터 없음** (PRD 열린 질문 → 미지원으로 확정). 지역 분해가 필요하면 `T7` 구글 트렌드로 보완한다.

#### ⚠️ 상대지수 정규화 — 이 데이터셋 최대의 함정

응답 `ratio`는 **"요청에 포함된 카테고리들 × 요청 기간" 안에서의 최댓값을 100으로 한 상대값**이다. 요청 묶음이 다르면 **스케일이 달라 서로 비교할 수 없다.**

두 가지 규칙으로 방어한다.

1. **기간을 쪼개지 말 것.** 3년 전체를 `timeUnit=date` 한 번에 요청한다. 부득이 분할하면 30일 이상 겹치는 구간을 두고 겹침 구간 비율로 체이닝(chaining)한다.
2. **앵커 카테고리를 넣을 것.** 3개 슬롯 중 1개를 모든 요청에 공통으로 넣는 기준 카테고리(예: `패션의류` 대분류)로 고정하고, 나머지 2개만 교체한다. 수집 후 앵커 대비 비율로 재스케일링하면 전체 카테고리를 하나의 축에 올릴 수 있다.

```
요청1: [앵커, 여성의류, 남성의류]
요청2: [앵커, 가방,   신발]
→ 저장 시 rescaled = ratio / anchor_ratio  (같은 날짜 기준)
```

이 처리를 하지 않으면 **카테고리 간 비교가 전부 무효**가 된다. 반드시 원본 `ratio`와 `rescaled`를 둘 다 저장한다.

#### 카테고리 cid

대분류는 확정값, 중분류는 수집 시 확인이 필요하다.

| 대분류 | cid |
|---|---|
| 패션의류 | `50000000` |
| 패션잡화 | `50000001` |
| 화장품/미용 | `50000002` |
| 스포츠/레저 | `50000007` |

중분류 cid는 카테고리 조회 API가 없다. `https://search.shopping.naver.com` 카테고리 페이지 URL 또는 데이터랩 웹 UI의 네트워크 요청 페이로드에서 추출해 `configs/categories.yaml` 에 기록한다. **출처 URL을 주석으로 남길 것.**

### T2. 카테고리 × 성별·연령·기기

```
POST /v1/datalab/shopping/category/device
POST /v1/datalab/shopping/category/gender
POST /v1/datalab/shopping/category/age
```

요청 형식은 T1과 동일. "한파 때 반응하는 연령대가 다른가" 같은 질문에 쓴다. 없어도 본류 분석은 가능하므로 P1.

### T3. 쇼핑인사이트 — 키워드별 클릭지수 ★ 세분 타깃

```
POST /v1/datalab/shopping/category/keywords
Body: { "startDate":..., "endDate":..., "timeUnit":"date",
        "category": "50000167",
        "keyword": [ {"name":"패딩","param":["패딩"]}, ... ] }   // 최대 5개
```

- **요청당 키워드 최대 5개** → 날씨민감 30개 = 6회 요청 (앵커 포함 시 8회)
- 중분류("여성의류")는 너무 뭉뚱그려져 날씨 효과가 희석된다. **패딩·트렌치코트·우산 같은 실제 신호는 이 데이터셋에 있다.**
- T1과 동일한 정규화 함정이 그대로 적용된다. 앵커 키워드 1개 고정 필수.

### T4. 검색어트렌드 (쇼핑 밖 검색 수요)

```
POST https://openapi.naver.com/v1/datalab/search
Body: { "startDate":..., "endDate":..., "timeUnit":"date",
        "keywordGroups":[ {"groupName":"패딩","keywords":["패딩","롱패딩"]} ] }  // 그룹 최대 5, 그룹당 키워드 20
```

- 2016-01-01~ 조회 가능
- 쇼핑 클릭(T1/T3)은 **구매 직전** 신호, 검색어트렌드는 **관심 단계** 신호다. 둘의 시차를 보는 것 자체가 Q4의 좋은 재료다.

### T5. 카테고리 인기검색어 TOP500 (순위형) — ⚠️ 비공식

데이터랩 웹의 "인기검색어" 순위는 **공식 오픈API에 없다.** 웹 UI 내부 엔드포인트(`datalab.naver.com/shoppingInsight/getCategoryKeywordRank.naver`)로만 얻을 수 있다.

- 순위 데이터가 필요한 이유: "한파 다음날 TOP500 진입 신규 키워드"는 강력한 스토리가 된다
- 리스크: 비공식 = 언제든 차단·변경 가능, ToS 회색지대
- **판단**: 없어도 프로젝트가 성립한다. 시도하되 막히면 즉시 접고 T3로 대체한다. 요청 간격 ≥ 3초.

### ~~T6. 네이버 쇼핑 검색 API~~ — 폐기

`/v1/search/shop.json` 은 **2026-07-31자로 완전 종료**됐고 공식 대체 API가 없다. 책·전문자료 검색도 함께 종료됐다.

상위 노출 상품 스냅샷은 이 프로젝트에서 포기한다. 순위형 신호가 필요하면 `T5`(비공식, 실패 허용)뿐이며, 그마저 안 되면 **`T3` 키워드 클릭지수의 일별 순위를 직접 매겨** 대체한다 — 지수 기반 순위는 공식 데이터로 재현 가능하므로 오히려 방어하기 쉽다.

### T7. 구글 트렌드 — 유일한 서울 지역 분해 소스

```python
pytrends.build_payload(["패딩"], timeframe="2023-01-01 2026-07-31", geo="KR-11")  # KR-11 = 서울
```

- 토큰 불필요, **비공식 라이브러리** (429 잦음 → 요청 간격 필수)
- 네이버 데이터랩에 지역 필터가 없으므로, "서울 한정 분석"이라는 PRD 전제를 데이터로 뒷받침할 수 있는 **유일한 소스**다
- 전국(`KR`) 대비 서울(`KR-11`) 지수 차이를 보면 "서울에 국한한 분석이 타당한가"를 검증할 수 있다
- 역시 0~100 상대지수 — 요청 단위 정규화 함정 동일

---

### 씨드 수집에서 실측된 데이터 특성 (2026-07 · 2026-08-18 수집)

문서상 스펙이 아니라 **실제 응답에서 확인된 것**들이다. 3년 백필 때 훨씬 크게 드러난다.

**① T3는 클릭이 적은 날의 행을 통째로 누락시킨다 — `ratio=0`이 아니라 행 자체가 없다.**

2026-07 기준 롱패딩 28/31일, 레인코트 1/31일, **방수자켓은 0건이라 parquet에 아예 존재하지 않는다.** 그대로 join하면 조용히 NaN이 되어 평균·상관이 왜곡된다. `processed` 단계에서 날짜축을 reindex한 뒤 **"결측 = 0인가, 미관측인가"를 명시적으로 결정**해야 한다.

겨울 키워드를 여름에 뽑으면 이 현상이 심해지므로, 3년 백필에서는 계절 반대 구간이 통째로 비는 시리즈가 생긴다. 이 결정은 분석 결과를 바꾸므로 노트북에 근거를 남길 것.

반대로 **T4(검색어트렌드)는 희소 키워드도 31/31일을 채워 반환한다.** 두 소스의 결측 규약이 다르다.

**② 앵커 재정규화의 필요성은 T3·T4에서 실증됐다.**

T3·T4는 앵커('반팔티')의 raw `ratio`가 배치마다 실제로 달랐다 → 재정규화 없이는 비교 불가.
T1은 배치 간 raw `ratio`가 우연히 일치했는데, 이는 API 성질이 아니라 **앵커('패션의류')가 항상 최대 카테고리라 분모가 같았을 뿐**이다. 앵커보다 큰 카테고리가 배치에 들어오면 스케일이 바뀐다 — "T1은 raw를 그냥 써도 된다"고 일반화하면 안 된다.

**③ T3의 `rescaled`는 카테고리를 가로질러 비교할 수 없다.**

T3는 "카테고리 안에서의" 상대값이라 cid가 다르면 앵커도 다르다(패션의류=반팔티 vs 패션잡화=양말). **카테고리를 넘는 비교는 T4를 써야 한다.**

**④ 네이버 실제 카테고리 트리는 원안과 다르다.**

가방·신발은 단일 중분류가 없고 성별로 갈린다(4개로 분리). 우산은 중분류가 아니라 소분류(`50001482`)다 — API가 소분류 cid도 받으므로 그대로 수집한다. **"액세서리"는 정확한 대응이 없어 주얼리(`50000189`)로 택했다 — 재검토 여지 있음** (후보: 패션소품 `50000185`, 헤어액세서리 `50000184`).

---

## 3. 보조 데이터

| ID | 내용 | 획득 |
|---|---|---|
| `C1` | 공휴일 | 공공데이터포털 한국천문연구원 특일 정보 `getRestDeInfo` (연 단위 호출) |
| `C2` | 미세먼지 일평균 | 에어코리아 API는 과거 조회가 제한적 → **최종확정 측정자료 CSV 다운로드**가 현실적 |
| `C3` | 온라인쇼핑동향 (의복·신발, 월별 거래액) | KOSIS 공유서비스 API. **검색지수가 실제 거래액과 같이 움직이는지 검증**하는 ground truth |

`C3`는 분량은 작지만 가치가 크다. "검색지수 상승이 실제 매출 상승과 상관 0.7" 한 줄이 분석 전체의 신뢰도를 올린다.

---

## 4. 필요한 API 토큰

| # | 발급처 | 토큰 | 용도 | 한도 | 승인 |
|---|---|---|---|---|---|
| 1 | [기상청 API 허브](https://apihub.kma.go.kr) | `authKey` | W1~W4 **전부** | 계정별 일일 한도 (콘솔 확인) | 즉시, **휴대폰 등록 필수** |
| 2-A | [네이버 개발자센터](https://developers.naver.com/apps) | `Client ID` + `Client Secret` | T1~T4 (유예 2027-06-30) | 데이터랩 1,000회/일 | 즉시 |
| 2-B | NAVER API HUB (네이버클라우드 콘솔) | `X-NCP-APIGW-API-KEY-ID` + `-API-KEY` | T1~T4 (2-A 불가 시) | 각 월 50,000건, 50 RPS | 콘솔 신청 |
| 3 | [공공데이터포털](https://www.data.go.kr) | `serviceKey` | C1 공휴일, C2 미세먼지 | 개발계정 10,000회/일 | 즉시~1시간 |
| 4 | [KOSIS](https://kosis.kr/openapi/) | `apiKey` | C3 (선택) | 넉넉 | 즉시 |

**2-A와 2-B는 택일이다.** 먼저 `python scripts/check_keys.py` 로 2-A가 살아있는지 확인하고, FAIL이면 2-B를 발급한다. HUB의 데이터랩 경로는 공개 문서에 없으므로 NCP 콘솔의 API 문서에서 실제 경로를 확인해 `scripts/check_keys.py` 의 추정 경로를 수정해야 한다.

**발급하지 않아도 되는 것**

- **카카오** — 쇼핑하우 공개 API가 **존재하지 않는다.** 카카오 개발자 오픈API의 검색은 웹/블로그/카페/이미지/동영상/책뿐이라 쇼핑 카테고리 데이터를 얻을 수 없다. PRD 4.2의 카카오 항목은 **삭제하거나 "카카오 웹·블로그 검색으로 정성 신호 대체"로 수정**해야 한다.
- **구글 트렌드** — 토큰 없음 (pytrends)

### 네이버 앱 등록 시 주의

애플리케이션 등록 후 **사용 API에 `데이터랩(쇼핑인사이트)`과 `데이터랩(검색어트렌드)`을 각각 추가**해야 한다. 둘은 별도 권한이라 하나만 체크하면 다른 쪽이 401로 떨어진다. 환경은 `WEB` / 서비스 URL `http://localhost`.

### 호출량 산정 (3년 백필 총량)

| 데이터셋 | 호출 수 | 근거 |
|---|---|---|
| W1 ASOS | ~43 | 31일 제약, 43개월 |
| W2·W3 중기예보 | ~86 | 월 단위 × 2종 |
| W4 특보 | ~4 | 연 단위 조회 |
| T1 카테고리 | ~10 | (14 중분류 + 앵커) ÷ 2 슬롯 |
| T3 키워드 | ~8 | 30 키워드 ÷ 4 슬롯 |
| T4 검색어트렌드 | ~8 | 30 키워드 ÷ 4 그룹 |

**총 200회 미만.** 데이터랩 일일 한도(1,000)에 여유가 크므로, 실패 시 재시도와 검증 호출을 아끼지 않아도 된다.

---

## 5. 저장 레이아웃

```
data/
├── raw/
│   ├── kma_asos/2023-01.parquet …            # W1
│   ├── kma_fct_temp/2023-01.parquet …        # W2
│   ├── kma_fct_land/2023-01.parquet …        # W3
│   ├── kma_warning/2023.parquet …            # W4 (연 파티션)
│   ├── datalab_category/2023-01.parquet …    # T1
│   ├── datalab_keyword/2023-01.parquet …     # T3
│   ├── datalab_search/2023-01.parquet …      # T4
│   └── google_trends/2023-01.parquet …       # T7
├── processed/
│   ├── weather_daily.parquet
│   ├── weather_forecast_daily.parquet        # 발표일-대상일 쌍
│   ├── shopping_trend_daily.parquet
│   └── derived_event.parquet
└── external/
    ├── monsoon_official.csv                  # 장마 공식 발표일 (수동)
    ├── holidays.csv                          # C1
    └── kosis_online_shopping.csv             # C3
```

`raw/` 에는 **API 응답을 최소 가공만 해서** 넣는다(정규화·조인 금지). rescaling·파생변수는 전부 `processed/` 단계에서 한다 — 규칙이 바뀌어도 API를 다시 때리지 않기 위해서다.

### 제공 시 함께 넘길 것

각 데이터셋마다 **수집 메타데이터**를 같이 남긴다. 없으면 6개월 뒤 재현이 불가능하다.

```yaml
# data/raw/datalab_category/_meta.yaml
collected_at: 2026-08-20
source: naver_datalab_shopping_categories
request:
  startDate: 2023-01-01
  endDate: 2026-07-31
  timeUnit: date
  anchor_category: "50000000"   # 재스케일링 기준
  batches: [[앵커, 여성의류, 남성의류], ...]
notes: ratio는 배치별 상대값. rescaled 컬럼이 앵커 기준 재정규화 결과.
```

---

## 6. 확정 사항 요약 (PRD 반영 필요)

| 항목 | 결론 |
|---|---|
| 데이터랩 지역 필터 | **미지원 확정.** 쇼핑 데이터는 전국 단일 → 서울 기상 × 전국 수요 구조임을 리포트에 명시. 지역 검증은 구글 트렌드로 보완 |
| 중기예보 백필 | **가능** (2012-12~). 발표분 아카이브를 쓰면 실전 조건 모델이 됨. 단 실제 호출로 검증 우선 |
| 기상특보 백필 | **가능** (2004-06~). 룰 기반 이벤트 정의의 검증 정답지로 활용 |
| 카카오 쇼핑하우 | **API 없음.** PRD에서 삭제 또는 대체 |
| 네이버 쇼핑 검색 API | **2026-07-31 종료, 대체 없음.** T6 폐기 |
| 네이버 데이터랩 | **API HUB 이관.** 기존 방식은 2027-06-30까지 유예 → 프로젝트 기간 내 사용 가능 |
| 순위(TOP500) 데이터 | 공식 API 없음. 비공식 경로는 P2, 실패 허용. 최후 대체는 T3 지수 기반 자체 순위 |

---

## 7. 산출 스키마

수집기가 만드는 테이블 정의. 코드 주석이 이 절을 참조한다.

```sql
weather_daily (                 -- W1 지상관측
  date DATE, region STRING,
  t_max FLOAT, t_min FLOAT, t_avg FLOAT,
  precip_mm FLOAT, humidity FLOAT, wind FLOAT,
  weather_code STRING           -- CA_TOT/RN_DAY/SD_NEW 파생. raw 에서는 NULL
)

weather_forecast_daily (        -- W2·W3 중기예보 발표분 아카이브
  tmfc TIMESTAMP,               -- 발표시각 (1일 2회: 06/18 KST)
  target_date DATE,             -- 예보 대상일
  mod STRING,                   -- A01(24h) / A02(12h) — A02는 한 대상일에 00·12시 두 행
  region STRING,
  t_max FLOAT, t_min FLOAT, rn_st FLOAT, sky STRING, wf STRING
)
-- ★ 조인 규칙: 최소 리드타임 3일(실측 2.2~9.8일). "발표일 D-1 → 대상일 D" 는 성립하지
--   않는다. 대상일 D 에 대해 `tmfc < D` 인 발표분 중 가장 최신을 고른다(실제로는 D-3).

weather_warning (               -- W4 기상특보
  reg_id STRING,                -- 서울은 4개 권역(L1100100~400)으로만 발표
  wrn STRING,                   -- C한파 H폭염 R호우 S대설 D건조 W강풍 (실측 검증)
  lvl STRING,                   -- 1주의 2주의보 3경보
  cmd STRING,                   -- 1발표 3해제 6변경  ← 해제는 별도 행
  tm_fc TIMESTAMP, tm_ef TIMESTAMP
)
-- TM_ED(해제시각) 컬럼은 존재하지 않는다. 발효구간은 발표행과 해제행을 짝지어 조립한다.

shopping_trend_daily (          -- T1·T3·T4
  date DATE, source STRING,     -- naver_datalab_category | _keyword | _search
  category_l1 STRING, category_l2 STRING, keyword STRING NULL,
  ratio FLOAT,                  -- API 원본 0~100 (배치별 상대값 — 배치 간 비교 불가)
  rescaled FLOAT,               -- 앵커 대비 재정규화 ← 분석은 이걸 쓴다
  batch_id STRING, cid STRING, is_anchor BOOL,
  gender STRING NULL, age_group STRING NULL
)

derived_event (                 -- 룰 기반 생성 (transform 단계)
  date DATE, region STRING, event_type STRING, phase STRING
)
```
