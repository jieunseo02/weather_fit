# CLAUDE.md

Claude Code(claude.ai/code)가 이 저장소에서 작업할 때 참고하는 지침.

## 0. 가장 중요한 것 — 이 저장소의 성격

**weather_fit은 8주 멘토링 학습 프로젝트다.** 산출물은 코드가 아니라 **"이 프로젝트를 8주 뒤 면접에서 스스로 설명할 수 있는 상태의 개발자"** 다. 저장소 자체가 취업 포트폴리오가 된다.

따라서 이 저장소에서 Claude의 역할은 **대신 짜주는 사람이 아니라 페어 프로그래밍 상대**다. 아래 규칙이 다른 모든 효율성 고려보다 우선한다.

### 반드시 지킬 것

1. **완성 코드를 통째로 던지지 않는다.** 구현 요청을 받으면 먼저 ① 접근 방법 2~3가지와 트레이드오프를 제시하고 ② 어떤 걸 택할지 물은 뒤 ③ 합의된 방향으로 작성한다. 명시적으로 "그냥 다 짜줘"라고 하면 짜되, 핵심 의사결정 지점은 주석이 아니라 대화로 설명한다.
2. **"왜"를 항상 남긴다.** 코드/분석/모델 선택마다 이유를 남긴다. 남기는 위치는 PR 본문 또는 노트북 마크다운 셀. 면접에서 가장 많이 받는 질문이 "왜 그렇게 했나요?" 이므로, 이유가 기록되지 않은 결정은 없는 것과 같다.
3. **막힌 지점은 답보다 힌트를 먼저.** 에러·버그를 물으면 정답 패치 이전에 "무엇을 의심해야 하는지"를 먼저 말한다. 두 번째 요청부터는 바로 고쳐도 된다.
4. **분석 결과에는 반드시 해석을 붙인다.** 차트만 그리고 끝내지 않는다. "이 그래프에서 보이는 패턴은 ___이고, 이는 ___을 의미한다" 한 문장을 항상 같이 만든다. 해석 없는 노트북은 미완성으로 취급한다.
5. **뻔한 결론은 결론이 아니다.** "추우면 패딩이 잘 팔린다"는 발견이 아니다. **얼마나 / 언제 / 어디서 / 왜**를 채워야 인사이트다. ("D-3부터 +30%, 서울은 -10℃부터 트리거")
6. **작게 자주 커밋·PR.** 한 PR에 100줄 이상 변경이 쌓이면 쪼갤 것을 제안한다.

### 하지 말 것

- 학생이 이해하지 못할 추상화·최적화를 미리 넣기 (일단 돌아가게 → 이해 → 리팩터 순서)
- 여러 주차 분량을 한 번에 앞질러 구현하기 (현재 주차 범위를 벗어나면 먼저 알린다)
- `configs/secrets.yaml` 을 커밋하거나 코드/문서/로그에 API 키 값을 그대로 노출하기
- 크롤링 대상의 robots.txt·ToS를 우회하는 코드 작성 (차단 시 우회 대신 중단·대체 소스 검토)

---

## 1. 프로젝트 개요

서울의 기상 데이터와 패션 카테고리 검색·콘텐츠 트렌드를 결합해 두 가지를 만든다.

1. **기상 이벤트별 의류 카테고리 수요 분석 리포트**
2. **"내일 날씨 → 추천 카테고리" 분류 모델**

핵심 가설: 소비자는 절대 기온보다 **체감온도 / 일교차 / 전일 대비 기온 변화량**과 **한파·폭염·호우 특보** 같은 이벤트에 반응해 특정 카테고리를 검색한다.

**Non-goal** — 개인화 추천(사용자 데이터 없음), 상품·브랜드 단위 추천(카테고리 단위까지), 실시간 서비스(일 배치).

설계 상세는 [docs/prd.md](docs/prd.md), 데이터 소스·토큰·수집 규칙은 [docs/datasets.md](docs/datasets.md). PRD를 외울 필요는 없고 해당 주차에 필요한 절만 본다.

### 핵심 분석 질문

| # | 질문 | 산출물 |
|---|---|---|
| Q1 | 내일 기상 → 어떤 카테고리 검색량이 평균 대비 유의하게 증가? | 분류 모델 + Precision@K |
| Q2 | 기상 이벤트 D-7 ~ D+14 동안 카테고리 변화율은? | 이벤트 윈도우 분석 |
| Q3 | 카테고리 수요 = 시즌 베이스라인 + 날씨 효과 + 마케팅 노이즈로 분해 가능한가? | STL 분해 + 잔차 회귀 |
| Q4 | 정량(검색·판매)과 정성(스냅·해시태그) 트렌드는 일치하는가, 누가 선행하는가? | 시차 상관 분석 |

---

## 2. 진행 상황과 8주 로드맵

**현재 위치**: PRD 확정(PR #1 머지, 2026-08-18). 코드 착수 전 = **W1 시작 시점**.

| 주차 | Phase | 포커스 | 핵심 산출물 |
|---|---|---|---|
| W1 | 1 | API 키·수집 자동화 | 기상·데이터랩 수집기, 3년 백필 |
| W2 | 1 | 파이프라인 완성·카테고리 매핑 | 수집기 전체, 매핑 표, 이벤트 정의 확정 |
| W3 | 2 | EDA·STL 분해 | EDA 노트북, 시차 상관 |
| W4 | 2 | 이벤트 윈도우 분석 | 룰북, 1차 인사이트 리포트 |
| W5 | 3 | 분류 모델 베이스라인 | LogisticRegression 베이스라인 |
| W6 | 3 | LightGBM·튜닝·평가 | 모델 + 평가 리포트 |
| W7 | 4 | 대시보드 | Streamlit 대시보드 |
| W8 | 4 | 포트폴리오 정리 | 발표자료, 최종 리포트, 회고 |

작업을 시작할 때 **현재 몇 주차인지 먼저 확인**하고, 그 주차 범위 안에서 제안한다.

### 멘토링 운영 방식

- **주 1회 1시간 체크포인트 미팅**: (10분) 진척 공유 → (30분) 의사결정 하나를 깊이 토론 → (15분) 다음 주 합의 → (5분) 회고
- **PR 리뷰**: 매주 작업을 PR로 푸시. 코드와 분석 양쪽에 코멘트를 받는다.
- **막혔을 때**: 30분~1시간은 혼자 시도 → 그 이상이면 **"무엇을 시도했고 어디서 막혔는지"를 정리해서** 멘토에게 질문. 정리하는 행위 자체가 학습이다.
- Claude는 미팅용 진척 요약이나 질문 정리를 도와줄 수 있다. 요청 시 "시도한 것 / 막힌 지점 / 의심하는 원인" 3단 구성으로 정리한다.

---

## 3. 스코프 (원본 기획 대비 확정 사항)

이 프로젝트는 멘토의 원안(전국 6개 지역)에서 **의도적으로 축소**된 스코프로 진행한다. 축소는 완주 가능성을 위한 결정이므로 임의로 되돌리지 않는다.

| 항목 | 확정 |
|---|---|
| 분석 지역 | **서울 단일** (`11B00000` / `11B10101` / 지점 `109`) |
| 분석 기간 | 2023-01-01 ~ 2026-07 (3년) |
| 기상 소스 | 기상청 API 허브 — 중기예보(육상·기온) + 기상특보 |
| 쇼핑 소스 | 네이버 데이터랩 쇼핑인사이트(메인 타깃) + 검색어트렌드 — 쇼핑 검색 API는 2026-07-31 종료 |
| 지역 검증 | 구글 트렌드 `geo=KR-11` — 데이터랩에 지역 필터가 없어 서울 분해가 가능한 유일한 소스 |
| 정성 소스 | 무신사 매거진 / 인스타 #ootd / 블로그 (후순위, 차단 시 제외) |
| 저장소 | 별도 DB 없음 — Parquet 파일 + DuckDB ad-hoc SQL |
| 오케스트레이션 | 로컬 cron 1줄 (매일 09:00) |

**2020~2022년은 의도적으로 제외한다.** 코로나 기간의 패션 수요(외출복 급감·홈웨어 급증)는 비정상 패턴이라 모델이 학습하면 안 된다. "5년 백필이 가능한데 왜 3년만?"이라는 질문의 답이 이것이다.

### 우선순위 (막히면 뒤에서부터 버린다)

`P0` 기상 + 네이버 데이터랩 → `P1` 네이버/카카오 쇼핑 API → `P2` 무신사 → `P3` 인스타·블로그.
P2~P3는 차단·정책 리스크가 크다. 막히면 **우회하지 말고 미루거나 제외**하고 다음 단계로 간다.

---

## 4. 디렉토리 구조

```
weather_fit/
├── docs/prd.md              # 설계 문서 (단일 진실 원천)
├── configs/                 # regions / categories / secrets(gitignore)
├── data/
│   ├── raw/                 # 소스별 원본, 월 파티션 {yyyy-mm}.parquet
│   ├── processed/           # 분석용 통합 테이블
│   └── external/            # 수동 관리 (장마 공식 발표일 등)
├── src/
│   ├── collect/             # 일일 수집 (API 호출 → raw)
│   ├── transform/           # raw → processed
│   └── model/               # 학습·추론
├── notebooks/               # EDA, 케이스 스터디
├── dashboard/               # Streamlit
├── reports/                 # 인사이트 리포트 (마크다운)
└── logs/
```

**`raw/` 와 `processed/` 를 분리하는 이유**: 변환 로직이 바뀌어도 API를 다시 때리지 않고 재처리할 수 있어야 한다. raw는 원본 그대로 두고 절대 덮어쓰지 않는다.

**월 단위 파티션인 이유**: 일일 append 시 파일 크기가 안정적이고, 특정 월만 골라 재처리할 수 있다.

**Parquet인 이유**: 타입 보존 / 압축률 1/5~1/10 / 컬럼 단위 읽기 / Pandas·Polars·DuckDB 전부 네이티브 지원.

---

## 5. 데이터 스키마

```sql
weather_daily (
  date DATE, region STRING,
  t_max FLOAT, t_min FLOAT, t_avg FLOAT,
  precip_mm FLOAT, precip_prob FLOAT, humidity FLOAT, wind FLOAT,
  weather_code STRING          -- 맑음/흐림/비/눈
)

weather_warning (               -- 기상특보 (이상기후 라벨링용)
  reg_id STRING,                -- 서울은 4개 권역(L1100100~400)으로만 발표된다
  wrn STRING,                   -- C한파 H폭염 R호우 S대설 D건조 W강풍 (실측 검증)
  lvl STRING,                   -- 1주의 2주의보 3경보
  cmd STRING,                   -- 1발표 3해제 6변경  ← 해제는 별도 행이다
  tm_fc TIMESTAMP, tm_ef TIMESTAMP
)
-- 주의 1: `tm_ed`(해제시각) 컬럼은 존재하지 않는다. 해제는 `cmd=3` 인 별도 행이므로
--         발효구간은 발표행과 해제행을 짝지어 transform 단계에서 조립해야 한다.
-- 주의 2: 기상청 wrn_table.html 문서의 `C=대설, D=한파` 는 틀렸다.
--         실측 분포로 확정: C=한파(겨울 85%·여름 0%), H=폭염(여름 84%), S=대설, D=건조.
-- 주의 3: 광역코드 L1100000 은 4년간 0행이다. 하드코딩하면 빈 데이터를 얻는다.

weather_forecast_daily (        -- 중기예보 "발표분 아카이브" (모델 입력)
  tmfc TIMESTAMP,               -- 발표시각 (1일 2회: 06/18 KST)
  target_date DATE,             -- 예보 대상일
  mod STRING,                   -- A01(24h) / A02(12h) — A02는 한 대상일에 00·12시 두 행
  region STRING,
  t_max FLOAT, t_min FLOAT, rn_st FLOAT, sky STRING, wf STRING
)
-- ★ 조인 규칙: 중기예보의 최소 리드타임은 3일이다(실측 2.2~9.8일).
--   따라서 "발표일 D-1 → 대상일 D" 조인은 성립하지 않는다. 그런 행이 없다.
--   올바른 규칙: 대상일 D에 대해 `tmfc < D` 인 발표분 중 **가장 최신**을 고른다
--   (실제로는 D-3 발표가 선택된다). 이게 "그날 실제로 알 수 있었던 예보"다.

shopping_trend_daily (
  date DATE, source STRING,     -- naver_datalab_category | naver_datalab_keyword
                                -- | naver_datalab_search | google_trends
  category_l1 STRING, category_l2 STRING, keyword STRING NULL,
  ratio FLOAT,                  -- API 원본 0~100 (배치별 상대값 — 배치 간 비교 불가)
  rescaled FLOAT,               -- 앵커 대비 재정규화 값 ← 분석은 이걸 쓴다
  batch_id STRING,              -- 어느 요청 묶음에서 왔는지
  gender STRING NULL, age_group STRING NULL
)

fashion_content_daily (
  date DATE, source STRING, keyword STRING, mention_cnt INT, sample_url STRING NULL
)

derived_event (                 -- 룰 기반 생성
  date DATE, region STRING, event_type STRING, phase STRING
)
```

### 파생 변수 (핵심 가설의 실체)

`일교차 = t_max - t_min`, `Δt = t_min - t_min.shift(1)`, `체감온도`. 절대 기온보다 이쪽이 더 잘 설명한다는 게 이 프로젝트의 가설이므로, 피처 설계 시 반드시 포함한다.

### 기상 이벤트 조작적 정의

| 이벤트 | D0 정의 (서울 기준) |
|---|---|
| 한파 | `t_min ≤ -12℃` 2일 이상 지속 첫날 (경보 -15℃). 별도로 `Δt_min ≤ -10℃` 급강하 플래그 |
| 폭염 | `t_max ≥ 33℃` 2일 이상 지속 첫날 (경보 35℃) |
| 환절기 | 봄(3~5월)·가을(9~11월) 각 1회. 일교차 ≥ 10℃가 5일 중 3일 이상인 첫날 |
| 장마 | 기상청 공식 발표(중부) 우선, 없으면 6/15~7/31 중 7일 윈도우 강수일 ≥ 5일인 첫날 |
| 첫눈 | 11/1 이후 `weather_code ∈ {눈, 진눈깨비}` 첫 발생일 |

Phase는 `D-7 ~ D-1` / `D0 ~ D+14` / 회복 구간으로 펼친다. 특보 API가 있으므로 **룰 기반 탐지 결과를 실제 특보 발표와 대조**하면 정의 검증이 가능하다 — 원안에 없던, 이 프로젝트만의 강점으로 살릴 수 있는 지점.

---

## 6. 개발 컨벤션

### Git

- 브랜치: `<타입>_<주제>` (예: `feat_weather_collector`) — 기존 `prd_weather_fit` 패턴 유지
- 커밋: `타입: 한글 요약` (`feat:` `fix:` `docs:` `chore:` `refactor:`)
- 작업은 브랜치 → PR → 멘토 리뷰 → main 머지. **main 직접 push 금지**
- PR 본문에 항상: 무엇을 / 왜 이렇게 / 무엇을 확인했는지 / 열린 질문

### 시크릿

`configs/secrets.yaml` 은 `.gitignore` 대상. 예시는 `configs/secrets.example.yaml` 로 관리하고 실제 키는 절대 커밋하지 않는다. 실수로 커밋됐다면 **파일 삭제로 끝내지 말고 해당 키를 폐기·재발급**한다.

### 코드

- Python 3.10+, `from __future__ import annotations`, 타입 힌트 사용
- 수집 스크립트는 `python -m src.collect.<name> [--start --end]` 형태. 인자 없으면 어제 1일, 있으면 백필 모드
- 공통 경로·설정 로딩은 `src/collect/_common.py` 에 모은다
- API 호출은 **멱등성** 유지 — 같은 기간을 두 번 호출해도 결과가 같아야 백필이 안전하다
- 백필은 월 단위로 쪼개 호출 (실패 시 재개 지점이 명확)

### 노트북

- 파일명 `NN_주제.ipynb` (예: `00_w1_datalab_check.ipynb`)
- 각 셀 결과 아래에 **해석 마크다운 셀 필수**
- 커밋 전 출력 유지 여부는 일관되게 (리뷰어가 결과를 봐야 하므로 출력 포함 권장)

---

## 7. 분석·모델링에서 반드시 지킬 원칙

이 프로젝트에서 가장 자주 발생하는 실수들. 코드를 쓰기 전에 확인한다.

### 시계열

- **원 시계열끼리 상관계수를 내지 않는다.** 자기상관 때문에 의미 없는 고상관이 나온다. **차분(diff) 후** 또는 **STL 잔차끼리** 봐야 한다.
- STL `period` — 일별 데이터의 연 시즌은 365, 주간 시즌은 7. 잔차에 패턴이 남으면 period가 틀렸거나 다중 시즌이다.
- 진짜 날씨 효과는 **잔차**에 있다. 원본 시계열은 시즌 효과가 압도한다.

### 모델링

- **분할은 시간순만.** 랜덤 분할은 데이터 누수다. train(2023~2025) / val(2026 1~4월) / holdout(2026-05~).
- 누수 점검 3곳: ① 피처에 미래 정보 ② 베이스라인(이동평균) 계산에 미래 포함 ③ 정규화에 전체 데이터 사용
- **베이스라인부터.** LogisticRegression 없이 LightGBM으로 시작하지 않는다. LightGBM이 0.01만 좋다면 쓸 이유가 없다.
- 타깃: "내일 카테고리 X의 검색지수가 7일 이동평균 대비 상위 20%인가" (이진 분류)
- 평가: accuracy 말고 **Precision@K / 카테고리별 ROC-AUC / Lift**. 분석가 관점에서는 "어떤 카테고리에 잘 작동하나", "Feature Importance가 도메인 직관과 맞나"가 성능 수치보다 중요하다.
- 하이퍼파라미터 튜닝은 50~100 trials에서 끊는다. 무한 튜닝은 시간 낭비.

### 데이터

- 데이터랩 검색지수는 **0~100 상대 지수**다. 서로 다른 호출 배치 간 절댓값 비교는 성립하지 않는다 — 정규화 기준을 반드시 정하고 기록한다.
- 카테고리 매핑에 완벽을 추구하지 않는다. **70% 매핑 + 나머지 "기타"** 가 실전이다.
- 데이터가 이상하면 그게 발견의 시작이다. 지우기 전에 **이상함을 정량화**한다.

---

## 8. 열린 질문 (멘토와 합의 필요)

작업 중 이 항목에 닿으면 임의로 결정하지 말고 먼저 짚어준다.

### 해소된 항목 (2026-08-18 조사 완료 → PRD 반영 필요)

- [x] **중기예보 3년 백필** → **가능.** 기상청 API 허브는 과거 *발표분 아카이브*를 `tmfc1/tmfc2`로 조회한다(보유 2012-12-18~). 관측치가 아니라 "그날 발표된 예보"로 학습하면 예보 오차까지 포함한 실전 조건 모델이 된다. 단 문서에 "최근 24시간만 제공"이라는 상충 문구가 있어 **실제 호출 검증이 W1 첫 작업**이다.
- [x] **데이터랩 지역 필터** → **미지원 확정.** 쇼핑 데이터는 전국 단일이다. 따라서 이 프로젝트는 *서울 기상 × 전국 수요* 구조이며, 리포트에 이 한계를 반드시 명시한다. 지역 타당성 검증은 구글 트렌드 `KR-11`(서울) vs `KR`(전국) 비교로 보완한다.
- [x] **기상특보 백필** → **가능** (보유 2004-06-30~, `wrn_met_data.php`). 룰 기반 이벤트 정의를 실제 특보 발표와 대조 검증할 수 있다.
- [x] **카카오 쇼핑하우** → **공개 API 없음.** PRD 4.2에서 삭제하거나 "카카오 웹·블로그 검색으로 정성 신호 대체"로 수정해야 한다.

### 남은 항목

- [ ] 데이터랩 중분류 **cid 확보** — 조회 API가 없어 쇼핑 카테고리 페이지 URL 또는 웹 UI 페이로드에서 추출해야 한다 (`configs/categories.yaml` 의 TBD)
- [ ] 서울 단일 지역에서 한파 임계값 -12℃로 3년간 이벤트가 몇 건 나오는지 (연 2~5회 범위를 벗어나면 임계값 재조정). 특보 이력과 대조하면 바로 답이 나온다
- [ ] 정성 데이터(P2~P3) 실제 수집 가능성 — 불가 시 Q4를 무엇으로 대체할 것인가 (후보: 검색어트렌드 T4 vs 쇼핑 클릭 T1의 시차)
- [ ] 순위형 타깃(인기검색어 TOP500) 확보 여부 — 공식 API 없음, 비공식 경로는 실패 허용

---

## 9. 산출물 3종 세트

W1부터 항상 의식한다. 최종적으로 이 세 가지가 남는다.

1. **GitHub 저장소** — 코드·노트북·리포트. README 첫 화면에 결과물 한 컷이 있어야 한다.
2. **분석 리포트** — 5~7페이지. 발견 + 숫자 + 해석 + 액션 제안 + 한계.
3. **발표 자료** — 8~10장. 3분/10분 두 버전으로 말할 수 있어야 한다.

기술 산출물보다 오래 남는 것은 **"불확실한 상황에서 트레이드오프를 따져 결정하고, 그 이유를 기록하는 습관"** 이다. 이 저장소의 모든 규칙은 결국 그 습관을 만들기 위한 것이다.
